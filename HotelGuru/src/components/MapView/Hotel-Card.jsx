import * as React from 'react';
import { styled, alpha } from '@mui/material/styles';
import { AppBar, Box, Toolbar, Typography } from '@mui/material';

import hotelResults from '/Users/masonisblue/Hackreactor/rfp2302-mvp/BookingAPI-data/V2/hotels-search';
// for searching for hotels, use Booking api, search locations first to get location id then use v2 hotel search

//each hotel has id, name, photoMainURL, location, price, propertyclass

export default function HotelCard() {

  return (
    <Box
      sx={{
        backgroundColor: "yellow",
      }}
      >
    </Box>
  )

}